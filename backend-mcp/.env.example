# CAD engine HTTP endpoint (local CAD API used for model operations).
CAD_ENGINE_URL=http://localhost:5000
# LLM parsing service endpoint (local NLP/assistant API).
LLM_SERVICE_URL=http://localhost:7000
